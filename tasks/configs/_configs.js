const folders = {
  sass: 'sass',
  stylesheets: 'css',
  javascript: 'js',
  images: 'img'
};

const paths = {
  source: './source',
  build: './build',
  dist: './dist'
};

export {paths, folders};
